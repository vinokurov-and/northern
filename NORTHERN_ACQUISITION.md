# Northern → Total: канал привлечения

- **Автор:** pm (Claude)
- **Дата:** 2026-04-19
- **Статус:** Черновик стратегии. Не ТЗ на имплементацию — ТЗ выходит после приёма этого документа.
- **Связанные OKR:** O2 KR2.3 (deep-link preselect), O2 KR2.1 (анонимный прогноз — уже за флагом в Total).

---

## 0. TL;DR

Northern сегодня — витрина клуба «Северный» с расписанием/командами/игроками, ранжируется в Яндексе. Посетители приходят узнать расписание или посмотреть игроков и уходят. Конверсии в Total (прогноз) — нет.

Предложение: встроить точечные CTA «Угадай счёт» на карточку матча (основной driver) и на страницы команд, запилить deep-link `/app/g-tournament?gameId=N` с предвыбором матча, мерить воронку «клик CTA → `/app/*` → анон-прогноз → регистрация».

**Жёсткое ограничение:** 242-ФЗ (сервер Total в Амстердаме) делает массовое привлечение трафика рискованным до переезда в РФ. До переезда — только работа над **качеством** воронки на имеющемся trickle-трафике, без промо и без инвайтов широкой аудитории. После переезда канал разворачивается в рост.

**Последовательность:**
1. **Сейчас** (пока Total в нидерландах): deep-link + один CTA на карточке матча, чистка трекинга. Скромно, без промо.
2. **После переезда в РФ:** второй CTA на странице команды, персонализация по returning-юзеру, эксперименты с баннером на главной.

---

## 1. Аудит northern

### 1.1. Текущие страницы

| Страница | Путь | Источник данных | Трафик | Нужное для Total |
|---|---|---|---|---|
| Главная | `/` | парсинг `kfl-football.ru/tournament/1033939`, DatoCMS, SQLite players/news | неизвестно (нет GA, только `page_views` по `/app/*`) | уже парсит матчи «Северного» — `home/guest/date/score/kflUrl`; `GamesAnnouncementScreen` закомментирован (`src/pages/index.js:61`) |
| Расписание (внутри главной) | — | то же | то же | есть данные, нет UI |
| Карточка матча | `/games/[slug]` | DatoCMS `game` — `teamHome/guestTeam/date/pointHome/pointGuest/stadium` | нестатичный content-набор из DatoCMS, предположительно низкий трафик | **основной CTA «Угадай счёт»** |
| Все игроки | `/players` | SQLite `players` | — | CTA нет смысла (нет связи игрок→матч в Total) |
| Карточка игрока | `/players/[slug]` | DatoCMS `player` + SQLite | — | CTA нет смысла |
| Новости | `/news` | VK wall.get → SQLite `news` | — | возможна реклама Total в шаблоне новости (out of scope) |
| Статистика | `/stats` | — | — | возможно, общий CTA «Ставь прогнозы на весь сезон» |
| Аналитика | `/analytics` | `/c/analytics` | служебная | не трогаем |

**Ключевой gap:** на `/games/[slug]` и в закомментированном `GamesAnnouncementScreen` выводятся матчи, но нет ни одного linka в Total (`/app/*`). GameCard имеет только `<a href={game.kflUrl}>Перейти к анонсу</a>` на kfl-football.ru — трафик уходит на сторонний сайт.

### 1.2. Сильные стороны northern для воронки

- **SEO-ранжирование.** Страницы команды/матчей ранжируются в Яндексе по запросам «фк северный калуга расписание», «калужская футбольная лига» — это тёплая аудитория, ищущая именно расписание.
- **Single-PoP контента о КФЛ.** Northern парсит `kfl-football.ru` и показывает результаты — это даёт предлог показать CTA «А спрогнозируй сам» прямо рядом с будущим/прошедшим матчем.
- **Shared-домен с Total.** `/app/*` — это тот же origin `fc-sever.ru`, нет CORS, нет cross-domain cookies. `anonSessionId` из AsyncStorage (localStorage в web-билде Total) можно создавать прямо с northern до клика по CTA — см. §4.3.
- **Уже есть page-view трекинг.** `/app/*` пишется в `page_views`, `referer` сохраняется — полвоспонки можно посчитать без доработок.

### 1.3. Слабые места

- **Нет GA/Яндекс.Метрики на northern.** Трафик на `/games/[slug]`, `/players/[slug]` не считается. Перед запуском CTA — **обязательно** поставить Метрику (это ~10 минут, бесплатно, не требует согласования). Без неё невозможно оценить эффективность канала.
- **DatoCMS-games не синхронизирован с КФЛ-календарём.** На `/games/[slug]` — старые/вручную заведённые матчи (из DatoCMS), а на главной — live-парсинг КФЛ. Две разные сущности. Это значит, что CTA на `/games/[slug]` будет показывать только post-factum матчи (с `pointHome/pointGuest`), что практически бесполезно для прогноза (он на **будущий** матч). **Главный источник live-матчей** — парсер КФЛ на главной, его и надо соединять с Total.
- **Нет маппинга northern↔Total gameId.** У Total в БД `games (id, datetime, home, guest, ...)` — без `externalId`. Team имеет externalId/sourceId (привязка к КФЛ), а у games — нет. Для deep-link нужен маппинг «пара команд + дата на northern → Total `games.id`». См. §3.
- **`output: 'export'` (SSG).** Next.js генерирует статику, значит CTA-линк должен быть либо статически известен на build-time (тогда маппинг gameId делается в `getStaticProps`), либо клиентски (fetch при рендере). SSR нет. Это влияет на архитектуру CTA.

---

## 2. CTA — макет и размещение

### 2.1. Приоритеты размещения

| # | Место | Приоритет | Когда | Ожидаемый CTR |
|---|---|---|---|---|
| 1 | Карточка матча в `GamesAnnouncementScreen` на главной (сейчас закомменчено) | **MUST — первая волна** | когда включаем канал | средний — матч один, CTA виден сразу |
| 2 | Блок «Ставки на матч» на `/games/[slug]` | SHOULD — первая волна | тоже первая волна, если маппинг gameId уже есть | низкий (страница показывает завершённые матчи) |
| 3 | Виджет «Ближайший матч» в шапке/футере всех страниц | COULD — после переезда в РФ | когда снят 242-ФЗ-риск | высокий охват, низкий CTR |
| 4 | Баннер на `/players/[slug]` — «Спрогнозируй его следующий матч» | NICE — потом | — | нужна связь игрок↔ближайший матч команды, не примитивно |
| 5 | Баннер на главной над fold'ом | NO — **после переезда** | массовое привлечение опасно до РФ | — |

### 2.2. Макет CTA — карточка матча на главной

Текущий компонент: `src/screens/GamesAnnouncmentGames/GameCard/GameCard.js`.

**Сейчас:**
```
[date] [tournament]
[home] [score] [guest]
[Перейти к анонсу → kfl-football.ru]
```

**Предлагается (если матч ещё не состоялся — `score` пустой или "–:–"):**
```
[date] [tournament]
[home] [vs] [guest]
[Угадай счёт →] /app/g-tournament?gameId=N      ← primary CTA
[Анонс →] kflUrl                                ← secondary, мельче
```

**Если матч завершён (есть score):** CTA не показываем. Показывать «угадай счёт» на сыгранном матче — либо вводит в заблуждение, либо требует отдельного post-match режима (out of scope).

**Копирайт.** «Угадай счёт» > «Сделай прогноз» — проще, триггерит любопытство, не требует объяснений. Альтернатива для A/B после переезда: «Поставь на матч бесплатно», «Твой прогноз — твой рейтинг».

**Визуально.** Primary-кнопка (брендовый цвет Total), не просто ссылка. Высота ≥44px (mobile tap-target). Text-align center. Размер шрифта 16px.

### 2.3. Макет CTA — `/games/[slug]`

Ограничение: там DatoCMS-games, большинство — прошедшие. Делаем **один кейс**: если матч не состоялся (даты > now и нет `pointHome/pointGuest`) — показать CTA «Угадай счёт →». Иначе — не показывать. Это 80% случаев «не показывать», но один случай в месяц, когда страница годится для CTA, должен её отрабатывать.

### 2.4. Конверсия копирайта (гипотезы для A/B после переезда)

- «Угадай счёт» (baseline, нейтрально)
- «Поставь на матч» (ассоциации с букмекерами — может оттолкнуть, а может поднять CTR за счёт знакомого паттерна)
- «Прогноз за 10 секунд» (снимает порог «долго/сложно»)
- «Бесплатно, без регистрации» (отвечает на главное возражение)

До переезда в РФ — **не A/B-тестим**, трафика нет, статзначимость не собрать. Запускаем один вариант — «Угадай счёт» + подпись мелким «без регистрации».

---

## 3. Deep-link: `/app/g-tournament?gameId=N`

### 3.1. Текущее состояние

- Маршрут `GUEST_TOURNAMENT_SCREEN = /app/g-tournament` уже существует (`client/routers.ts`).
- Экран `GuestGamesScreen` (`client/screens/GuestGamesScreen/GuestGamesScreen.tsx`) показывает `<GuestTournaments />` — список всех турниров.
- **`gameId` из query сейчас не читается.** Предвыбор конкретного матча не реализован (это KR2.3).

### 3.2. Что нужно на клиенте Total (ТЗ для dev-а, не в этой задаче)

1. В `GuestGamesScreen` читать `gameId` из query (через `useLocalSearchParams` / `route.params`, зависит от навигатора).
2. Если `gameId` есть — автоматически: (а) раскрыть турнир, где этот матч; (б) прокрутить к карточке матча; (в) визуально подсветить (border / micro-animation 600ms), чтобы юзер сразу увидел, куда пришёл.
3. Если `gameId` невалидный (матч не найден, прошёл) — показать toast «Матч не найден» и показать обычный список.
4. Трекать событие `deep_link_landed` с полями `gameId`, `source=northern`, `found=true/false`.

### 3.3. Маппинг northern↔Total gameId

**Проблема.** Northern парсит КФЛ-календарь и получает `{home, guest, date, kflUrl}`. У Total в БД — `games (id, datetime, home, guest)`. Связи нет.

**Варианты:**

| Вариант | Описание | Плюсы | Минусы |
|---|---|---|---|
| A. Матчинг по `(home, guest, date±1ч)` на build-time northern | В `getStaticProps` главной делаем запрос `GET api.fc-sever.ru/api/games?home=X&guest=Y&date=Z` и подставляем `gameId` в CTA | Статика как есть, SSG не ломается | Ребилд northern нужен каждый раз, когда Total создаёт новую игру; рассинхрон |
| B. Клиентский fetch на northern перед переходом | Клик по CTA → JS fetch к Total API → редирект на `/app/g-tournament?gameId=N` | Всегда свежий маппинг | Один extra network round-trip; без JS не работает (но без JS и прогноз не работает, так что ок) |
| C. Добавить `kflUrl` в Total `games` и матчить на лету | Total при парсинге игр сохраняет `games.kflUrl` (или `games.kflGameId`). Northern передаёт `?kflUrl=` или `?kflGameId=`, Total сам резолвит | Чистый stable id | Нужна миграция в Total (`ALTER TABLE games ADD COLUMN kflGameId`); парсер в Total должен начать писать это поле |
| D. Редирект-ручка на Total `GET /api/resolveGame?home=X&guest=Y&date=Z → 302 /app/g-tournament?gameId=N` | Northern делает `<a href="https://api.fc-sever.ru/api/resolveGame?...">`, Total редиректит | Никакого JS на northern, SSR-safe | Ещё один endpoint в Total; latency на клике |

**Рекомендация:** **C (долгосрочно) + B (мост на переходный период).**

- Сейчас — B: клиентский fetch. Быстро запускается, не требует миграции в Total.
- Параллельно — заводить в беклог Total мини-задачу «добавить `games.kflGameId`, парсер заполняет его» (маленькая миграция + правка cron-парсера, ~1 день). После этого — переходим на C: deep-link вида `/app/g-tournament?kflGameId=12345`, клиент Total сам резолвит.
- C лучше B, потому что (а) не зависит от availability API при рендере/клике (в SSG важно); (б) URL стабилен и индексируется.

**Тонкий момент.** В варианте C deep-link `/app/g-tournament?kflGameId=12345` можно класть как единственный источник правды, а саму страницу `/app/g-tournament` обогатить server-side handler'ом в Total (endpoint `/api/page` уже обрабатывает `/app/*` и подтягивает OG-теги — можно расширить). Но это уже в IMPL.

### 3.4. Что делаем *до* переезда в РФ

Вариант B (client-side fetch). Никаких миграций Total, никаких ребилдов northern при новых матчах. Минимальный surface.

---

## 4. UX предвыбора

### 4.1. Happy path

1. Юзер на northern главной, видит «Сегодня: Северный vs Спартак Калуга».
2. Жмёт «Угадай счёт».
3. Редиректится на `/app/g-tournament?gameId=123` (или `?kflGameId=...`).
4. За ≤1 сек видит: открытый турнир, подсвеченный матч «Северный vs Спартак», кнопки П1/Х/П2 и поля счёта.
5. Ставит прогноз → анон-режим сохраняет → toast «Прогноз сохранён, войдите чтобы не потерять».

### 4.2. Edge cases

| Кейс | Что делаем |
|---|---|
| Матч уже начался | Анон-прогноз отбрасываем (серверная валидация setForecast), показываем «Матч уже идёт — прогноз нельзя. Посмотри другие матчи». |
| Матч найден, но турнир не активен (завершён) | Показываем в read-only, без кнопок. |
| `gameId` невалидный | Toast + fallback на обычный список `/app/g-tournament`. Трекаем `deep_link_landed {found: false}`. |
| Юзер уже залогинен (returning) | Перекидываем не в `g-tournament`, а в `/app/tournament?gameId=N` (юзерский путь) — CTA должен проверять наличие `sessionId` в localStorage и переписывать target на лету. |
| Медленный мобильный интернет | Нужен loader на `/app/g-tournament` пока грузится турнир. Сейчас Total grubs loader — проверить. |

### 4.3. Pre-seeding `anonSessionId` на northern

Идея на подумать (не MUST): при открытии любой страницы northern уже генерировать `anonSessionId` UUID v4 и класть в `localStorage['total_anonSessionId']`. Когда юзер кликает CTA и Total стартует — Total читает этот же ключ (общий origin) вместо того, чтобы генерить свой. Плюс: **activation-метрика в Total получит первый touch-point раньше** (на northern, а не на первом визите в `/app/*`). Минус: это информация, которую мы кладём пользователю без его действия, на странице, где Total не упоминается. Минорный privacy-шум. Обсуждать в отдельной ветке, но не сейчас.

---

## 5. Воронка и метрики

### 5.1. Шаги воронки

| Шаг | Событие | Источник события | Текущее состояние |
|---|---|---|---|
| 1. Визит на northern (любая страница) | `yametrika: page view` | Яндекс.Метрика | **нет Метрики** — надо ставить |
| 2. Показ CTA «Угадай счёт» | `cta_shown {page, gameId}` | custom — Метрика goal или endpoint northern | — |
| 3. Клик по CTA | `cta_click {page, gameId}` + отметка в `page_views` (referer=северная страница) | Метрика goal + существующий `page_views` | `page_views` уже пишется |
| 4. Приземление на `/app/g-tournament?gameId=N` | `deep_link_landed {gameId, source=northern, found}` | Total events | **новое событие**, см. §3.2 |
| 5. Анон-прогноз | `anon_forecast` | Total events (уже есть) | готово — флаг ON |
| 6. Регистрация | existing `signUp` event | Total | готово |
| 7. Merge анон→user | `merge_forecasts` | Total events (уже есть) | готово — флаг ON |

### 5.2. Метрики для дашборда

**На стороне Total** (в `/api/admin/metrics`, расширение существующего):

- `northernReferrals7d` — кол-во `deep_link_landed {source=northern}` за 7 дней.
- `northernCTR7d` = `deep_link_landed / cta_click` (должно быть ≈1.0; если <0.9 — JS ломает переход).
- `northernConversion7d` = `anon_forecast {source=northern} / deep_link_landed {source=northern}`.
- `northernActivation7d` = `signUp {came_from=northern within 24h} / anon_forecast {source=northern}`.

Все метрики — с `source=northern`, чтобы отделять от других каналов (direct, VK, telegram).

**На стороне northern** (через Метрику + `/c/analytics`):

- `cta_shown_total`, `cta_click_total` — чтобы посчитать CTR CTA на самом northern.
- `cta_click_by_page` — какая страница даёт больше кликов (главная vs /games/[slug]).

**Целевые ориентиры (до переезда в РФ — спекулятивно, до данных):**

- CTR CTA «Угадай счёт» на главной: ≥5% кликов от показов (для кнопки в контексте, видимой сразу).
- Anon-forecast conversion от landed: ≥40% (пришли с узким intent «хочу прогноз», барьер низкий — без регистрации).
- Registration conversion от anon-forecast: ≥25% (merge-логика уже работает — если юзер захотел сохранить прогноз).

Реальные цифры подтвердим после недели работы канала.

### 5.3. Связь с activation

Текущая activation-цель Total: 56% → 80% (PRD). Каналы trafic'а Northern привносят **прогрев**: юзер пришёл за расписанием, увидел матч, ткнул прогноз — classic low-friction activation. Ожидание: новые юзеры с northern-источника будут иметь **выше** activation, чем cold-start (прямой заход в Total), потому что у них уже есть intent «смотрю на этот матч». Гипотеза измеряема — через `activation_by_source`.

---

## 6. Риски

| # | Риск | Severity | Митигация |
|---|---|---|---|
| R1 | **242-ФЗ** — сервер Total в Амстердаме. Массовый трафик через Яндекс на российский футбольный клуб → Роскомнадзор видит накопление ПД нерезидентом | HIGH (блокер для раскрутки) | Первая волна — **только CTA на уже существующем трафике** (не покупаем, не промо'им). Полноценная раскрутка — после переезда. |
| R2 | **SEO-регресс.** Добавление CTA/JS на northern страницы может замедлить FCP, уронить Яндекс-ранжирование | MEDIUM | CTA — один HTML-элемент + один fetch при клике (не при рендере). Не добавлять тяжёлые iframe/трекеры. Метрика до/после — core web vitals в Метрике. |
| R3 | **Расхождение маппинга gameId.** Парсер northern и парсер Total работают независимо — могут видеть «разные» матчи в разные моменты | MEDIUM | Вариант C (общий `kflGameId`) снимает. Вариант B — полагается на `home/guest/date`, где date-парсинг фрагилен (timezone'ы, формат). Нужны unit-тесты маппера. |
| R4 | **Анонимный прогноз не работает у юзера без AsyncStorage/localStorage** (incognito, Safari ITP) | LOW | Fallback: если storage недоступен → скрыть CTA, не бросать юзера в сломанный флоу. Trackать `storage_unavailable`. |
| R5 | **Merge-конфликт при регистрации** (юзер уже имел аккаунт, теперь пришёл с northern и сделал анон-прогноз на матч, где уже стоял user-forecast) | LOW (уже решено) | Total А4: user wins, `conflictsCount` в ответе. Отрабатывает. |
| R6 | **Сломанный deep-link** после обновления Total (поменяли `/app/g-tournament` route) | LOW | Все link'и с northern — **одной функцией-хелпером** в `getCtaUrl(gameId)`. Менять URL — в одном месте. + E2E-тест на маршрут. |
| R7 | **Кеш next.js** после ребилда не подтягивает новые матчи | LOW | ISR / `fallback: 'blocking'` уже используется в `/games/[slug]`. Главная — полностью SSG, ребилдится при деплое northern, этого достаточно. |

---

## 7. Плохие идеи (чтобы не вернулись)

- **Полноценный форум/чат на northern** с «обсуди прогноз». Out of scope, не относится к acquisition, тянет поддержку.
- **Telegram-бот со ссылками на CTA.** До переезда в РФ любое публичное продвижение в мессенджерах = 242-ФЗ attention. После — можно.
- **Cross-posting Total-прогнозов в VK-стену FC Север.** Может оттолкнуть основную аудиторию northern (фанаты клуба, не все интересуются прогнозами). Тестировать очень аккуратно, отдельно.
- **Push-уведомления с northern** «начинается твой матч — ставь прогноз». Требует PWA инфры на northern, которой нет. Непропорциональная стоимость.

---

## 8. План работ

**Предусловия:** Total release (анон + merge) в проде — ✅ (коммит `0aed59b` на 2026-04-19). Метрика на northern — **не поставлена**, поставить первым шагом.

### Этап 1 — подготовка (до релиза канала, 1-2 дня)

1. Поставить Яндекс.Метрику на northern (все страницы). Цели: `cta_shown`, `cta_click`.
2. Раскомментировать `GamesAnnouncementScreen` на главной northern (`src/pages/index.js:61`).
3. В Total беклог: завести задачу на `games.kflGameId` + парсер (для будущего перехода на вариант C).
4. В Total беклог: завести задачу на `deep_link_landed` event в `GuestGamesScreen` (KR2.3 работа — совмещается).

### Этап 2 — минимальный канал (3-4 дня)

5. `GameCard.js` → добавить primary-кнопку «Угадай счёт».
6. `getCtaUrl(game)` — хелпер: если есть `sessionId` в localStorage → `/app/tournament?gameId=N`, иначе `/app/g-tournament?gameId=N`. Вариант B (client-side resolve). 
7. Клиент Total: читать `gameId` из query в `GuestGamesScreen`, предвыбирать матч. (Это dev-задача, не наша.)
8. `deep_link_landed` event + admin-метрики. (Dev.)
9. Smoke: один juicy матч с CTA, ручной проход flow по mobile+desktop.

### Этап 3 — измерение (неделя)

10. Ждём 7 дней трафика. Делаем ежедневный `SELECT COUNT(*) FROM events WHERE type='deep_link_landed' AND source='northern'`.
11. Собираем baseline: CTR CTA, conversion в anon-forecast, conversion в registration.
12. Если CTR <1% или conversion <5% — копирайт/дизайн CTA пересматриваем.

### Этап 4 — после переезда в РФ (когда)

13. Добавить второй CTA на страницу команды (ближайший матч команды).
14. A/B-тесты копирайта.
15. Виджет «Ближайший матч» в шапке.
16. Персонализация returning-юзеров (уже есть sessionId → путь в `/app/tournament`).

---

## 9. Out of scope

- Платный трафик (Директ, таргет в VK/TG).
- Новые страницы на northern (например, отдельная «прогнозы сезона»).
- Собственный feed/лента прогнозов на northern.
- Редизайн northern.
- Миграция Total в РФ (отдельный проект, блокер для §3.4 рост-сценариев).

---

## 10. Открытые вопросы к Андрею

1. **Метрика на northern.** Ставим Яндекс.Метрику? (рекомендую — бесплатно, обязательно для измерения). Если нет — нужен альтернативный способ трекать `cta_click` (custom endpoint в express-server).
2. **`GamesAnnouncementScreen` раскомментируем?** Почему был закомменчен — есть причина или просто забросили? (Если причина — надо её учесть, перед тем как возвращать в UI.)
3. **Переезд в РФ — timeline.** Когда планируется? От этого зависит timing Этапа 4 и объём работ второй волны.
4. **Вариант B vs C для deep-link.** Ок запустить сначала B (быстро), потом мигрировать на C (стабильно)? Или сразу C и подождать?
5. **`/games/[slug]` DatoCMS vs live-парсер КФЛ.** Стоит ли синхронизировать DatoCMS-games с КФЛ-календарём, или оставить DatoCMS для контентных страниц, а прогнозы вести только через главную?
